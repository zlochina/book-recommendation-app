server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  location /api/ {
    proxy_pass $BACKEND_HOST;
    rewrite ^/api/(.*)$ /$1 break;
  }
}

