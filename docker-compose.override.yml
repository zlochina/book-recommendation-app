version: "3.9"
services:
  backend:
    volumes:
      - ./src/book_recommendation_prod:/app/book_recommendation_prod
    command: >
      poetry run uvicorn book_recommendation_prod.main:app
      --host 0.0.0.0
      --port 8000
      --reload